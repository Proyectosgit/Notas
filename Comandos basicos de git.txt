git init
git add <file>
git status
git commit (git commit -m "Mensaje del commit")
git push
git pull
git clone

git diff

Working directory>>(git add)>>staging area>>(git commit)>>repository

--------------Para crear ramas
ver ramas
git branch
crear rama
git branch nombre de la rama
Seleccionar rama
git checkout 'nombre de la rama'
git remote add origin https://github.com/Direccion de git hub

------------------------------configuracion global
$ git config --global user.name "John Doe" //Usuario
$ git config --global user.email johndoe@example.com //Correo

------------------------------
desde la terminal del equipo donde esta el proyecto que queremos subir a github
Nos vamos a la carpeta del proyecto y ejecutamos estos comandos.
git init
git add .
git commit -m "first commit"
git remote add origin https://github.com/NOMBRE_USUARIO/NOMBRE_PROYECTO.git
git push -u origin master
